import { RestaurantValidationPipe } from './restaurant-validation.pipe';

describe('RestaurantValidationPipe', () => {
  it('should be defined', () => {
    expect(new RestaurantValidationPipe()).toBeDefined();
  });
});
